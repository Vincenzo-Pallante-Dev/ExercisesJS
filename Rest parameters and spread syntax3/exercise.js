const { id, ...otherInfo } = {
  id: 1,
  firstName: "Mario",
  lastName: "Rossi",
  age: 25,
};

console.log(id, otherInfo);

// const id = person.id;
// const personInfo = {
//   firstName: person.firstName,
//   lastName: person.lastName,
//   age: person.age
// };

// console.log(id, personInfo);
